# Database
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="lab_cafe"
POSTGRES_HOST="db"
POSTGRES_PORT="5432"
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"

# Auth.js / NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-32-char-secret"
GITHUB_ID=""
GITHUB_SECRET=""
ALLOWLIST_DOMAINS="example.com"

# Email (Notification Agent)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="Lab Cafe Hub <cafe@example.com>"

# App configuration
APP_CURRENCY="KRW" # Minor units (e.g., KRW, USD, EUR). Zero-decimal currencies are supported.
APP_LOCALE="ko-KR" # Controls default locale/number formatting in the UI.
LOW_STOCK_THRESHOLD_DEFAULT="6"
